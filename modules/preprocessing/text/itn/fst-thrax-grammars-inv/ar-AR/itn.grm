import 'inumerals.grm' as inumerals;
import 'idate.grm' as idate;
import 'icurrency.grm' as icurrency;
import 'itime.grm' as itime;
import 'iphone.grm' as iphone;

export INUMERALS = inumerals.MAIN; #normalize_numeral
export IDATE = idate.MAIN; #normalize_date
export ICURRENCY = icurrency.MAIN; #normalize_currency
export ITIME = itime.MAIN; #normalize_time
export IPHONE = iphone.MAIN; #normalize_phone
